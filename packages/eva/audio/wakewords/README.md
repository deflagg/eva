# Deprecated: wakewords assets

This directory is retained only for transition history.

Runtime wake-word assets (`.ppn`) are no longer used by Eva Audio.
Wake activation is transcript-based (`wake.phrases` in `packages/eva/audio/settings.yaml`).
