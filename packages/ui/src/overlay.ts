export interface OverlayBox {
  x: number;
  y: number;
  width: number;
  height: number;
}

export function drawOverlayPlaceholder(_ctx: CanvasRenderingContext2D, _boxes: OverlayBox[]): void {
  // Implemented in a later iteration.
}
